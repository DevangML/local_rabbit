{
  "name": "local-rabbit",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @local-rabbit/server dev",
    "build": "yarn workspaces run build",
    "start": "yarn workspace @local-rabbit/server start",
    "lint": "yarn workspaces run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.22.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "prismjs": "^1.29.0",
    "react-markdown": "^10.0.1",
    "remark-gfm": "^4.0.1"
  }
}