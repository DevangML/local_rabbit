{
  "name": "@local-rabbit/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint . --ext .ts,.js || true",
    "lint:fix": "eslint . --ext .ts,.js --fix || true",
    "lint:quiet": "eslint . --ext .ts,.js --quiet || true"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
  }
}