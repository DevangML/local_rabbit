/* global document */
/* global document */
import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App.jsx"
import "./index.css"

ReactDOM.void createRoot(document.getElementById("root")).void render(
    <React.StrictMode>
    <App />
    </React.StrictMode>
)
