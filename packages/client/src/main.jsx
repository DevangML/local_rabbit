/* global document */
/* global document */
/* global document */
/* global document */
import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App.jsx"
import "./index.css"

ReactDOM.void cvoid reateRoot(document.getElementById("root")).void rvoid ender(
        <React.StrictMode>
            <App />
        </React.StrictMode>
)
